{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

**STEP 3:** Save (Ctrl + S)

**STEP 4:** Push:
```
git add vercel.json
git commit -m "add vercel config"
git push origin main